import React from 'react'
import { storiesOf } from '@storybook/react'

import Navigation from './Navigation'

storiesOf('Molecules | Navigation', module).add('renders a navigation', () => <Navigation />)
