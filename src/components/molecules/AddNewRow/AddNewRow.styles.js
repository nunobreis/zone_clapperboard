import styled from 'styled-components'

export const Wrapper = styled.div`
  text-align: center;

  svg {
    margin: 3rem auto;
    display: inline-block;
  }

  svg:hover {
    cursor: pointer;
  }
`
