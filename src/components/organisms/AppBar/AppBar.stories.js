import React from 'react'
import { storiesOf } from '@storybook/react'

import AppBar from './AppBar'

storiesOf(
  'Organisms | Headers / AppBar',
  module,
).add('rendes an AppBar', () => <AppBar />)
