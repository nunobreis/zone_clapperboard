import React from 'react'
import { storiesOf } from '@storybook/react'

import AddRowIcon from './AddRowIcon'

storiesOf(
  'Atoms | Icons / Add Row Icon',
  module,
).add('renders a delete icon', () => <AddRowIcon />)
