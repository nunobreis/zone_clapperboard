import React from 'react'
import { storiesOf } from '@storybook/react'

import UploadIcon from './UploadIcon'

storiesOf(
  'Atoms | Icons / Upload Icon',
  module,
).add('renders an Upload Icon', () => <UploadIcon />)
