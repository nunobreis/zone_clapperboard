import React from 'react'
import { storiesOf } from '@storybook/react'

import AvatarIcon from './AvatarIcon'

storiesOf(
  'Atoms | Icons / Avatar',
  module,
).add('renders an Avatar Icon', () => <AvatarIcon />)
