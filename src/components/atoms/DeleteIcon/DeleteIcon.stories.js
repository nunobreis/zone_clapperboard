import React from 'react'
import { storiesOf } from '@storybook/react'

import DeleteIcon from './DeleteIcon'

storiesOf(
  'Atoms | Icons / Delete Icon',
  module,
).add('renders a delete icon', () => <DeleteIcon />)
